import { nestjsNodemailerMjml } from './nestjs-nodemailer-mjml';

describe('nestjsNodemailerMjml', () => {
  it('should work', () => {
    expect(nestjsNodemailerMjml()).toEqual('nestjs-nodemailer-mjml');
  });
});
